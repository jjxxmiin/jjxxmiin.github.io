# Bundler cache
.bundle
vendor
Gemfile.lock

# Jekyll cache
.jekyll-cache
.jekyll-metadata
_site

# RubyGems
*.gem

# NPM dependencies
node_modules
package-lock.json
!automation/package-lock.json

# IDE configurations
.idea
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/tasks.json

# Misc
_sass/vendors
assets/js/dist

__pycache__/*
__pycache__

.env

# 447개 리라이트 체크포인트·검증 원고 (로컬 지속 상태)
.rewrite-state/

# 서비스 계정 키 — 공개 저장소이므로 절대 커밋되면 안 된다.
GA4_CREDENTIALS_JSON.json
teachingflow-*.json
*-service-account*.json
*.credentials.json

# 썸네일 렌더용 임시 디렉터리 (snap Chromium이 /tmp 를 못 읽어 저장소 안에 둔다)
automation/thumb_tmp/

# 개인정보가 든 문서. 공개 저장소이므로 절대 커밋 금지.
# (생년월일, 주소, 연락처가 포함된 이력서 등)
*이력서*
*resume*.pdf
*_CV.pdf
